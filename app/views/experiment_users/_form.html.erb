<%= form_for(@experiment_user) do |f| %>
  <% if @experiment_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@experiment_user.errors.count, "error") %> prohibited this experiment_user from being saved:</h2>

      <ul>
      <% @experiment_user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :experiment_id %><br>
    <%= f.text_field :experiment_id %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.text_field :user_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
