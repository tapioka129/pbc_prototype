<%= form_for(@designs_user) do |f| %>
  <% if @designs_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@designs_user.errors.count, "error") %> prohibited this designs_user from being saved:</h2>

      <ul>
      <% @designs_user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :design_id %><br>
    <%= f.text_field :design_id %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.text_field :user_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
